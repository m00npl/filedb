{
  "name": "filesdb",
  "version": "1.0.0",
  "description": "File chunking service for Golem DB",
  "main": "src/server.ts",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "bun build src/server.ts --outdir=dist --target=bun",
    "start": "bun run dist/server.js",
    "test": "bun test"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.0",
    "hono": "^4.6.3",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest"
  }
}